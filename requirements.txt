fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
email-validator>=2.1
httpx>=0.27
celery>=5.4
redis>=5.0
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9
# Text-generation SDKs, one per selectable provider. All three are imported lazily, so a
# missing package degrades that slot to deterministic fallbacks rather than crashing boot —
# but they must be installed for the AI Provider board's choices to actually work.
anthropic>=0.116
# Also the only image-generation provider (Claude does not generate images).
openai>=1.40
google-genai>=1.0
python-dotenv>=1.0
# IANA timezone database. slim container images ship without one, and zoneinfo would then
# silently fall back to UTC — putting every scheduled post hours off.
tzdata>=2024.1
tenacity>=8.3
pyjwt>=2.9
cryptography>=42.0
python-multipart>=0.0.9
pytest>=8.2
ruff>=0.5
