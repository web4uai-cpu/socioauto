# Copy to .env.local and adjust. Never commit real values.

# Base URL of the backend API, including the /api/v1 prefix.
# Deployed backend (Railway):
VITE_API_BASE_URL=https://socioauto-production.up.railway.app/api/v1
# Local backend instead:
# VITE_API_BASE_URL=http://localhost:8000/api/v1
